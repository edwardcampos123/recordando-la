// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyC0wNeUY32DYrsnfY2xFCCteijex_Htybs",
    authDomain: "recordando-la-d1e3c.firebaseapp.com",
    databaseURL: "https://recordando-la-d1e3c.firebaseio.com",
    projectId: "recordando-la-d1e3c",
    storageBucket: "recordando-la-d1e3c.appspot.com",
    messagingSenderId: "1054298263208"
  }
};